<nav class="navbar navbar-expand-lg ">
    <a class="navbar-brand" href="{{ path('index') }}">Talmeet</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav">
            <li class="nav-item mr-4">
                <a class="nav-link" href="{{ path('index') }}">Home</a>
            </li>
            {% if app.user %}
                <li class="nav-item mr-4">
                    <a class="nav-link" href="{{ path('profile', {'username': app.user.username}) }}">Profile</a>
                </li>
                <li class="nav-item mr-4">
                    <a class="nav-link" href="{{ path('search') }}">Find talents</a>
                </li>
                <li class="nav-item mr-4">
                    <a class="nav-link login_out-link" href="{{ path('app_logout') }}">Logout</a>
                </li>
            {% else %}
                <li class="nav-item mr-4">
                    <a class="nav-link login_out-link" href="{{ path('app_login') }}">Login</a>
                </li>
                <li class="nav-item mr-4">
                    <a class="nav-link register-link" href="{{ path('app_register') }}">Register</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>